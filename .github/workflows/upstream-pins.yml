name: upstream-pins
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  placeholder:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "upstream-pins.yml is a stub - replaced in Task 6.2"
