# MAUI API

The public surface of `Qavren.Edge.Maui`, built as `net10.0-android`. `UseQavrenEdge()` on `MauiAppBuilder` is the entry point; the platform lifecycle bridge and `FileSystem`-backed paths are wired behind it.

| Namespace | Package |
|---|---|
| <xref:Qavren.Edge.Maui> | `Qavren.Edge.Core` |
