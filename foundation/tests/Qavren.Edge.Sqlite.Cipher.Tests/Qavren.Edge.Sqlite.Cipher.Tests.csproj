<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <OutputType>Exe</OutputType>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="xunit.v3" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
    <PackageReference Include="Microsoft.Extensions.Logging" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\Qavren.Edge.Sqlite\Qavren.Edge.Sqlite.csproj" />
    <ProjectReference Include="..\..\src\Qavren.Edge.Sqlite.Native.Cipher\Qavren.Edge.Sqlite.Native.Cipher.csproj" />
    <!-- Wave 5: the vector provider runs on whatever connection IEdgeDatabase hands it, SQLCipher
         included (spec 2 decision 10). This project is the only one in the suite that can prove
         that, because it is the one SP1 keeps out of the device lanes and the only one that may
         reference Qavren.Edge.Sqlite.Native.Cipher. -->
    <ProjectReference Include="..\..\..\embeddings\src\Qavren.Edge.VectorData\Qavren.Edge.VectorData.csproj" />
  </ItemGroup>
</Project>
